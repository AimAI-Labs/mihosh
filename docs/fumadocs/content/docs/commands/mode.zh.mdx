---
title: mihosh mode
description: 切换代理模式
---

## 说明

`mihosh mode <mode>` 命令用于切换 Mihomo 的代理模式。

## 支持的模式

- `rule`：规则模式，根据配置的规则分流。
- `global`：全局模式，所有流量走全局代理节点。
- `direct`：直连模式，所有流量直接连接，不走代理。

## 示例

```bash
mihosh mode rule
mihosh mode global
```
